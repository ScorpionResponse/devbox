---
# Provisioning a dev box

- name: Configuring Base System
  hosts: local
  tasks:
    - name: Checkout dotfiles
      git: repo=git://github.com/scorpionresponse/dotfiles.git dest=/home/{{ user }}/dotfiles

    - name: Install dotfiles
      shell: /home/{{ user }}/dotfiles/install_dotfiles.sh
